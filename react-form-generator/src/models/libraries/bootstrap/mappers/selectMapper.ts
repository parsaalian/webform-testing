// import { BootstrapSelect } from "../interfaces/select";
// import { ISelect, SelectOptions } from "../../../interfaces/inputs/select";
// import { ValidationStateEnum } from "../../../interfaces/primitives/validation";

/*
export class SelectMapper extends LibraryMapper implements BootstrapSelect {
    public label: string;
    public disabled: boolean;
    public isInvalid: boolean;
    public isValid: boolean;
    public value: any;
    public options: SelectOptions;

    constructor(select: ISelect) {
        super();
        const {
            label,
            disabled,
            validation,
            value,
            options,
        } = select;
        this.label = label ? label.value : '';
        this.disabled = disabled;
        this.isInvalid = validation && validation.state === ValidationStateEnum.INVALID || false;
        this.isValid = !this.isInvalid;
        this.value = value;
        this.options = options;
    }
}
*/